name: Hello World Pipeline
on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello World
        run: echo "👋 Hello, World! This is my first GitHub Actions pipeline."
      - name: print success
        run: echo "job success"
